import { StyleSheet } from "react-native";

export default StyleSheet.create({
  barTimeWrapper: {
    flex: 1,
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
  },
});
